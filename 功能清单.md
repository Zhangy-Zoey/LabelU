# LabelU Video — 功能清单与快捷键

Windows / macOS 桌面端：从视频中切出片段（可选画面裁切），或导入图片裁切保存，按类别导出；也可整段/整图批量归类。  
快捷键按 **Windows（Ctrl）** 书写；macOS 上 **Ctrl 可用 Command**。

---

## 一、主要功能

### 1. 导入与列表

- 打开文件夹 / 打开文件，或拖入窗口；递归加载常见**视频与图片**（jpg/png/webp/bmp/gif 等）
- 若目录**同时含视频与图片**：弹窗询问「只加载视频 / 只加载图片 / 都加载」；选都加载后，媒体列表旁有「全部 / 仅视频 / 仅图片」筛选
- 左侧缩略图列表；角上勾选、小窗预览（视频）、预览进度条
- 「只看未完成」过滤；「完成」在源文件名加 `_done` 标记（不另建文件），不改片内容
- 左右分栏可拖；拖到边缘可隐藏一侧（分隔条双击还原）
- 裁剪导出的片段/图片不会进列表；整段批量归入类别目录的会标「已归类」
- 已归类项可再分类：保存/批量默认写入「当前文件夹/类别名/」，可自选根目录（跨重启记住）；可「恢复默认」
- 异常自动写入 `exceptions.log` 并邮件上报；顶栏「查看日志」打开操作日志 `operations.log`；版本更新后异常日志覆盖清空
- GitHub Releases 自动更新（各平台只下载本平台安装包；客户端经国内镜像拉取）；macOS 未签名时下载后打开安装包并拖入「应用程序」；新版本首次打开弹出更新内容
- 会话级「不再询问」（批量结果、确认框、恢复会话、删标签、导入范围、更新详情）；保存分类与批量分类每次均需选择类别
- 无画面裁切且无时间裁剪时，分类直接移动原文件到目标目录（不复制副本）；有裁切/裁剪时仍导出新文件

### 2. 时间轴剪辑

- 拖入/出点手柄设选区；长片全览像素过粗、拖不到约 0.04 秒时弹窗说明原因，并可一键放大后再拖；点时间轴空白处移动白色播放头（非设选区）
- 拖入/出点手柄；松手靠近标记可吸附
- 选区至少 **约 0.04 秒**（按 ≥25fps 的一帧计；错误偏低的探测帧率不再把最短抬高），且须落在未分类剩余段内
- 导出：无裁切且选区 ≥0.5 秒时优先流拷贝（含 macOS）；短片 / 画面裁切 / 流拷贝失败则重编码（input seek + 短 preroll，避免从片头硬解）
- Windows 重编码优先硬件编码器（NVENC → AMF → QSV → libx264）
- 空格播放选区；「循环开/关」（默认开）
- 「预热开/关」：导入后后台为可能需代理的视频预生成兼容预览（可选）
- 保存可排队：导出进行中仍可再确认保存，串行执行
- 「轴」：全片 / 选区放大
- 倍速 0.5 / 1 / 2 / 4；步进 1 / 8 / 16 / 22
- 播放头白线可拖动预览
- **M** 打标记；点标记可删；**Alt+M** 清全部

### 3. 选帧与微调

- 拖入/出点后出现选帧条；中心格为精确入/出点，邻格按步进间距；点格定格（不自动播）
- `[` / `]` 按片源帧把播放头设为入/出点（可达约一帧下限）
- 选帧区高度可拖大；蓝/黄框对应入/出点
- 切换步进只改选帧间距与 ←→ 步长，不重吸附已有入/出点

### 4. 画面

- 播放区滚轮缩放；放大后可拖移；双击复位
- 「画面裁切」：进入后**不预设框**，在画面上按住拖拽框选；松手后可拖动/四角微调；再点可取消
- 已分类片段不可再裁切，需先删除该分类结果

### 5. 分类保存（核心）

- Enter 或「保存片段 / 保存图片」→ 先选分类任务（猫 / 狗 / 自定义）→ 选/新建类别标签 → 确认保存（默认「当前文件夹/类别名/」，可改根目录或恢复默认）
- 分类任务可切换；出厂「猫」「狗」可删可改名；「恢复猫狗」可补回缺失任务，并重置已有同名猫/狗的出厂大类与标签；自定义任务最多 3 个（新建为空）
- 大类/标签均可点击编辑名称与颜色（标签色优先于大类色）；改大类色时继承色标签即时预览，保存生效，点空白/Esc 放弃；可增删；同类内拖拽调序；删大类会带走其下标签
- 有裁切/时间裁剪时导出到 `根目录/类别名/`；文件名含源信息、序号、入出点、类别；有裁切再带 `_c…`
- 无裁切且选区覆盖整段（或图片无裁切）时：**移动原文件**到 `根目录/类别名/`，不另导出副本
- **图片**：无时间轴，画面裁切后保存（可多次裁切到不同类别）；命名规则与视频片段一致（扩展名为 jpg/png/webp）
- 裁剪导出**不写回、不覆盖原片**；重开靠文件名复原时间轴与裁切
- 彩色分类段可点预览；Delete / Backspace 删除后可重切
- 点「完成」，或用 ←/→ 切走时若已有分类会静默自动完成

### 6. 批量分类

- 勾选 / Ctrl+单击多选；Shift+单击从锚点连选；拖选追加（多次拖选保留已选）；可开「二次选择」再挑子集
- 整段视频或整张图片**移动**到 `根目录/类别名/`（与保存分类共用根目录偏好；可恢复默认）
- 失败可重试；Ctrl+Z 可撤（操作历史落盘，跨重启仍可用）

### 7. 撤回

Ctrl+Z / Ctrl+Shift+Z（macOS 可用 Command）：统一操作历史（含导出、删除片段、批量分类、选区/标记等），跨重启仍可用。

---

## 二、快捷键

| 快捷键 | 作用 |
|--------|------|
| **Enter** | 打开「保存片段」；保存弹窗内确认导出 |
| **空格** | 选区播放 / 暂停（循环开时到出点重播） |
| **Ctrl+Z** | 撤回上一步（操作历史，跨重启仍可用） |
| **Ctrl+Shift+Z** | 复原上一步 |
| **← / →** | 上一个 / 下一个视频（「只看未完成」时 → 会跳过已完成） |
| **微调中 ← / →** | 按「步进」移动当前正在调的入点或出点 |
| **Shift+← / Shift+→** | 非微调时：左调入点、右调出点（按「步进」） |
| **Alt+← / Alt+→** | 非微调时：按原片帧率逐帧移动播放头（不改选区） |
| **[ / ]** | 把当前播放头设为入点 / 出点（按片源帧对齐） |
| **M** | 在播放头添加标记 |
| **Alt+M** | 清除全部标记 |
| **单击标记** | 删除该标记 |
| **Delete / Backspace** | 删除当前选中的已分类导出片段 |
| **Esc** | 关闭保存窗；或退出微调/裁切/分类预览等（不清除入出点数值） |
| **Ctrl+单击** | 多选 / 取消选中单个视频 |
| **Shift+单击** | 从锚点连选到此处（中间全部选中） |
| **拖选** | 框选追加到已选（不取消已选） |
| **Ctrl+滚轮** | 缩放左侧缩略图 |
| 播放区**滚轮** | 缩放画面（不用 Ctrl） |
| 缩略图角上**勾选** | 多选（与 Ctrl+单击同类） |

### 连选用法（Shift+单击）

1. 先点第一个视频（普通点或 Shift+点均可，作为锚点）
2. **按住 Shift**，再点另一个视频
3. 两端之间（含两端）全部选中；锚点保持在第一次，可继续 Shift+点更远项

### 方向键注意

- **进入微调**（拖过入/出点手柄）后：←/→ 只 nudge 当前端；此时 Shift/Alt+方向键也走同一逻辑，不会切视频或逐帧移播放头
- **选中已分类彩色段**预览时：Shift/Alt/[ / ] 无效；←/→ 仍可切视频

### 弹窗

保存、批量、确认、恢复会话等弹窗打开时，导航与剪辑类快捷键会暂时屏蔽，避免误操作。
