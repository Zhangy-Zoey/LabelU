# LabelU Video 用户手册

从视频里切出片段、可选画面裁切，按类别保存；也可整段/整图批量归类。  
**裁剪导出**不改动原片；**整段分类 / 批量分类**会把文件**移动**到类别子目录。

快捷键按 **Windows（Ctrl）** 书写；macOS 上可用 **Command**。

---

## 1. 导入

- **打开文件夹 / 打开文件**：选视频、图片或含媒体的文件夹  
- 也可把文件/文件夹 **拖进窗口**  
- 若目录同时含视频与图片：会询问「只加载视频 / 只加载图片 / 都加载」  
- 左侧列表点条目即可编辑；勾选「只看未完成」可过滤已点过「完成」的条目  
- **已完成**的媒体也可点开回看：时间轴上会显示各分类片段（点彩色段可预览）  
- 重新打开已处理过的文件夹时，会根据类别子目录中的导出**文件名**（及时段/裁切后缀）与导出目录索引自动复原分类  
- Windows 无法在同一对话框同时选文件与文件夹，会先问导入方式；也可直接拖入

---

## 2. 标准流程（一条视频）

1. **拖时间轴两端手柄**设入/出点（点时间轴空白处只移动白色播放头）  
2. 需要画面裁切时点「裁切」，拖框选定区域  
3. **Enter** 或点「保存片段」→ 先选分类任务（猫 / 狗 / 自定义），再选类别标签（默认保存到「当前文件夹 / 类别名 /」，可改根目录）→ 确认  
4. 可继续切下一段；全部切完点「完成」（或按 → 自动完成）  
5. **← / →** 切到上/下一个视频

### 保存时会发生什么

- **有时间裁剪或画面裁切**：导出新文件到 `分类根目录 / 类别名 /`，原片不动  
- **选区覆盖整段且无画面裁切**（或图片未裁切）：**移动原文件**到 `分类根目录 / 类别名 /`，不另存副本  

导出文件名形如  
`源文件夹_源文件名_序号_s入点ms_e出点ms_类别.mp4`；若做了画面裁切，末尾再加  
`_c左_上_宽_高`（相对画面 0–1，万分比整数）。  
重开文件夹时**只靠文件名**复原时间轴与裁切。  
点「完成」会在源文件名加上 `_done` 标记（例如 `a.mp4` → `a_done.mp4`），**不另建标记文件、不改片内容**；撤销完成则去掉该后缀。  
类别子目录里的裁剪片段不会再出现在左侧列表（避免与源片混淆）。

选区至少 **约 0.04 秒**（按 ≥25fps 的一帧计），且只能落在未分类的灰色剩余段上。拖拽入/出点按 0.01 秒吸附可达该下限；选帧条中心格保留精确时间，邻格与 ←→ 微调仍按「步进」间距。无画面裁切且选区不少于约 0.5 秒时优先快速流拷贝导出（入点对齐选区内关键帧，相邻段接缝不重复）；更短选区、有裁切或流拷贝失败时会重编码（仍用快速定位，不必从片头解到入点）。  
「分类根目录」默认是当前媒体所在文件夹；可改并跨重启记住，弹窗内可「恢复默认」。实际落盘为 `根目录/类别名/`。

保存/批量弹窗顶部可切换**分类任务**（出厂「猫」「狗」，以及最多 3 个自定义任务）。当前任务会跨重启记住。大类标题、标签均可点击编辑名称与颜色；编辑大类颜色时，继承大类色的标签会即时预览，点「保存」写入，点空白处或 Esc 放弃未保存修改。标签可在同一大类内拖拽调序。「恢复猫狗」会补回缺失的出厂任务，并对已有同名「猫」「狗」重置为出厂大类/标签。

---

## 3. 时间轴

| 操作 | 怎么做 |
|------|--------|
| 移动播放头 | 点/拖时间轴空白处（白色竖线） |
| 设选区 | 拖入点（蓝）/ 出点（黄）手柄；长片全览过粗拖不到最短时会弹窗说明，可「放大后继续」 |
| 调入/出点 | 拖两端手柄；松手靠近标记时会吸附 |
| 播放选区 | **空格** |
| 循环播放 | 打开「循环开」后再空格 |
| 预热预览 | 「预热开」：导入后后台预生成 HEVC 等兼容预览（可关） |
| 看附近帧 | 拖手柄时下方出现选帧条；中心格为当前精确入/出点，点邻格按步进间距定点 |
| `[` / `]` | 把入/出点设到播放头（按片源帧对齐） |
| 放大选区 | 「轴」选「选区」；要看全片选「全片」 |
| 打标记 | **M** 或点「标记」；点标记可删；**Alt+M** 清全部 |
| 看已分类段 | 点彩色段预览；**Delete** 删除该分类结果后再重切 |

倍速、步进（1/8/16/22）在时间轴下方切换。步进越大，方向键微调越细。  
保存片段时可排队：上一次导出未完成时仍可再确认，会按顺序执行。

---

## 4. 画面

- 播放区 **滚轮**：缩放；放大后可拖移；双击复位  
- 「裁切」：拖框/四角；再点一次可取消  
- 已分类片段不能再裁切，需先 Delete 删除该结果

---

## 5. 批量分类

整段视频 / 整张图片直接归类（不裁切导出）：

1. 左侧 **Ctrl+单击** 多选，或 **Shift+单击** 连选，或 **拖选追加**（多次拖选会保留已选）  
2. 可选「二次选择」：从已多选中再挑要分类的子集（不打断小窗播放）  
3. 点「批量分类」→ 选类别（默认「当前文件夹 / 类别名 /」，可改根目录或恢复默认）→ 确认  
4. 文件会被**移动**到 `根目录/类别名/`（不是复制）  
5. 失败项可「重试」；可用 **Ctrl+Z** 撤回（操作历史落盘，跨重启仍可用）

---

## 6. 撤回

**Ctrl+Z** / **Ctrl+Shift+Z**（macOS 可用 Command）走统一操作历史：导出、删除片段、批量分类、选区/标记等。  
顶栏撤销按钮提示「跨重启仍可用」。

---

## 7. 快捷键

| 键 | 作用 |
|----|------|
| Enter | 保存片段 |
| 空格 | 播放/暂停选区 |
| Ctrl+Z | 撤回 |
| Ctrl+Shift+Z | 复原 |
| ← / → | 上/下一个视频 |
| [ / ] | 当前播放头设为入/出点 |
| Shift+← / Shift+→ | 微调入点 / 出点 |
| Alt+← / Alt+→ | 按原片帧率逐帧走播放头 |
| M | 加标记 |
| Alt+M | 清全部标记 |
| Delete | 删除选中的已分类片段 |
| Esc | 关保存窗 / 退出编辑态（分类编辑也可用点空白退出） |
| Ctrl+滚轮 | 缩放左侧缩略图 |

微调模式下（拖过手柄后）：← / → 按当前「步进」移动入或出点。

---

## 8. 注意

- 未点「完成」就退出：原片仍在；下次打开可恢复或丢弃本次已导出片段  
- 安装包未签名时：Windows 可能提示未知发布者（选「仍要运行」）；macOS 可能被 Gatekeeper 拦截（可右键「打开」）  
- 当前 macOS 安装包仅支持 Apple Silicon（arm64）；Windows 为 x64 NSIS 安装包  
- macOS 自动更新：下载后会打开安装包，需手动拖到「应用程序」覆盖（未签名无法静默安装）；Windows 可自动安装并重启  
- Windows 上完成/批量移动偶发「文件被占用」：多为杀毒或播放器仍锁文件，稍后重试或关掉预览再操作即可  
- 处理中不要关软件；长时间导出可点取消（已完成的批量项仍可撤回）
